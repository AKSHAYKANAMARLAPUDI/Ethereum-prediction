# -*- coding: utf-8 -*-
"""Untitled14.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1hwsdDWC09_6NI5VwaB16e0hQ7S4vqJzg
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from sklearn.linear_model import LinearRegression
# Load the historical data (replace placeholders with your actual paths)
eth_data = pd.read_csv("/content/ETH-USD.csv")
other_crypto_data = pd.read_csv("/content/BTC-USD.csv")

# Calculate correlations for a specific timeframe (e.g., daily)
correlations = eth_data["Close"].corr(other_crypto_data["Close"])

# Identify strongest and weakest correlations

correlations = pd.Series(correlations)  # Wrap correlations in a Series
strongest_correlations = correlations.sort_values(ascending=False).head(5)
weakest_correlations = correlations.sort_values().head(5)
print("Strongest Correlations:", strongest_correlations)
print("Weakest Correlations:", weakest_correlations)

# Visualize correlations over time (example for a single currency)

plt.plot(eth_data["Close"], label="ETH")
plt.legend()
plt.show()

# Incorporate correlations into prediction model (example using Linear Regression)
from sklearn.linear_model import LinearRegression
train_data = pd.concat([eth_data.set_index('Date')['Close'], other_crypto_data.set_index('Date')[eth_data]], axis=1).dropna()


# Separate features (X) and target variable (y)
X_train = train_data[['High']]  # Assuming 'ETH Close' is the column name for Ethereum prices in train_data
y_train = train_data['Close']



# Generate future dates from February 4th to February 10th, 2024
future_dates = pd.date_range(start="2024-02-04", end="2024-02-10")

# Placeholder prices for Ethereum and Bitcoin for the future dates
# Replace these placeholder values with your actual future price predictions
eth_future_prices = [1000.00, 1050.00, 1100.00, 1150.00, 1200.00, 1250.00, 1300.00]
btc_future_prices = [50000.00, 51000.00, 52000.00, 53000.00, 54000.00, 55000.00, 56000.00]

# Create future_data DataFrame
future_data = pd.DataFrame({
    "Date": future_dates,
    "ETH Close": eth_future_prices,
    "BTC Close": btc_future_prices
})

print(future_data)